require "spec_helper"

describe Templator do
  it "has a version number" do
    expect(Templator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
